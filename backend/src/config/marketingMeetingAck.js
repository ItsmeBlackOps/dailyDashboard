// Marketing-team one-time acknowledgment of the Tasks-Today meeting status mark.
// Bump `version` to re-prompt the marketing team after revising it.
export const MARKETING_MEETING_ACK = { version: 1 };
// Legacy role tokens (req.user.role is already legacy) for the marketing track.
export const MARKETING_MEETING_ACK_ROLES = ['admin', 'mm', 'mam', 'mlead', 'recruiter'];
