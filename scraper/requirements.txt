# Core HTTP + parsing
httpx[http2]==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0
feedparser==6.0.11

# Data + storage
pandas==2.2.3
python-dateutil==2.9.0.post0
SQLAlchemy==2.0.36
supabase==2.28.3

# JavaScript rendering (used sparingly, only when required)
playwright==1.48.0

# Config + CLI
python-dotenv==1.0.1
click==8.1.7
# supabase 2.28.3 → realtime 2.28.3 → pydantic >= 2.11.7
# Floor at 2.11.7, cap at <3 to avoid v3 breaking changes.
pydantic>=2.11.7,<3

# Observability
structlog==24.4.0
tenacity==9.0.0

# Testing
pytest==8.3.3

# JD enrichment via gpt-4o-mini (Responses API requires openai >= 1.50)
openai>=1.50.0
